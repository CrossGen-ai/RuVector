# 0001.yaml — ADR sidecar (src/core/plugins/core/schema/adr-sidecar.schema.yaml, version 1).
# Scaffolded by `cga-core-adr new`, then edited directly. Creating an ADR is gated
# (adr-via-cli); editing one is not. Add a row per TC-N in the .md — adr-lint
# checks both directions, so a criterion with no row is a lint failure.
adr: "0001"
title: "Cascade Asymmetric Distance Computation (Cascade-ADC): coherent 4-bit prune + 8-bit refine for PQ scan"
status: Proposed
added: 2026-09-05
updated: 2026-09-05
deciders: []
drivers: ""

verify:
  - tc: TC-1
    scope: observation
    target: "manual:tc-1-0001"
    note: "state WHY this target proves the criterion"
    observation_detail:
      method: "the exact procedure, reproducible by a person"
      stale_after_days: 180
    last_result: unrun

enforce: []
