[package]
name = "ruvector-tribase"
version = "0.1.0"
edition = "2021"
description = "Triangle-inequality pruning for graph-based ANN search (research PoC)"
license = "MIT OR Apache-2.0"

[dependencies]
rand = "0.8"
rand_chacha = "0.3"

[dev-dependencies]

[features]
default = []
