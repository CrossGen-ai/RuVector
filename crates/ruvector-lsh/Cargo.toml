[package]
name = "ruvector-lsh"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Bitsampling SimHash multi-probe LSH for cosine-similarity ANN: three-variant baseline with measured recall and latency"
keywords = ["vector-search", "ann", "lsh", "simhash", "cosine"]
categories = ["algorithms", "data-structures", "science"]

[[bin]]
name = "lsh_benchmark"
path = "src/bin/benchmark.rs"

[dependencies]
rand = { workspace = true }
rand_distr = { workspace = true }

[lints.rust]
dead_code = "allow"
unused_variables = "allow"
